{
  "name": "@eso-market-tracker/data",
  "version": "0.0.1",
  "description": "Holds our full data in an flat and accessible format.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "SEE LICENSE IN ../LICENSE.md",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm run test && tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "build:sqlite": "tsx cli.ts",
    "migrate:segments": "tsx migrate-observations.ts",
    "compress": "tsx compress-data.ts"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "cheerio": "^1.2.0",
    "dotenv": "^17.4.2",
    "p-limit": "^7.3.1",
    "playwright": "^1.62.0"
  },
  "dependencies": {
    "@eso-market-tracker/database": "workspace:*",
    "@eso-market-tracker/eso": "workspace:*",
    "@eso-market-tracker/logging": "workspace:*",
    "fast-glob": "^3.3.3",
    "fflate": "^0.8.3"
  },
  "exports": {
    ".": "./index.ts"
  }
}
