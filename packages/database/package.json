{
  "name": "@eso-market-tracker/database-management",
  "version": "2026.03.19.1c40eff",
  "description": "Allows manipulation of the flat file database",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "ISC",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm run test && tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0"
  }
}
