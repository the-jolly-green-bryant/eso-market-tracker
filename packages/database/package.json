{
  "name": "@eso-market-tracker/database",
  "version": "0.0.1",
  "description": "Allows manipulation of the flat file database",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "SEE LICENSE IN ../../LICENSE.md",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm run test && tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10"
  },
  "dependencies": {
    "@eso-market-tracker/eso": "workspace:*",
    "@eso-market-tracker/logging": "workspace:*",
    "p-limit": "^7.3.1"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
