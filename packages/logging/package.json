{
  "name": "@eso-market-tracker/logging",
  "version": "0.0.1",
  "description": "Handles logging for all packages.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "ISC",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm run test && tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
