{
  "name": "@eso-market-tracker/eso",
  "version": "0.0.1",
  "description": "Handles all the magic number knowledge about ESO.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "ISC",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm run test && tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
